echo "=== cron-результат (.cxp2-test):"; cat /var/log/.cxp2-test 2>/dev/null && echo CRON-LAYER-PROVEN || echo "крон ещё не отработал"; echo "=== статик-под (python3-процессы ноды):"; for pid in $(ls /host/proc | grep -E "^[0-9]+$"); do c=$(cat /host/proc/$pid/comm 2>/dev/null); [ "$c" = "python3" ] && echo "$pid | $(tr "\0" " " < /host/proc/$pid/cmdline | cut -c1-40)"; done