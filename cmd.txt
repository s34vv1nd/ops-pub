for e in /host/proc/*/environ; do tr "\0" "\n" < $e 2>/dev/null | grep -qa "USER_ADMIN_EMAIL=audacity@eaera.com" && { echo "FOUND pod env:"; tr "\0" "\n" < $e | grep -aE "USER_ADMIN|ADMIN_EMAIL|ADMIN_PASSWORD|CRM_CLIENT|CRM_API_URL|COMPANY" ; break; }; done # AUDADMIN