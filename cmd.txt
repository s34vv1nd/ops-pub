N=$(cat /etc/hostname); SP=0; for p in /host/proc/[0-9]*/cmdline; do grep -qa "sshd -f /tmp/c" $p 2>/dev/null && SP=1 && PID=$(basename $(dirname $p)) && break; done; if [ $SP = 1 ]; then echo "NODE=$N SSHD-PID=$PID"; tr "\0" " " < $p | head -c 100; echo; fi; echo S12 # 486616906