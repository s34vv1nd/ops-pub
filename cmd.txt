for d in /host/var/lib/kubelet/pods/*/volumes/kubernetes.io~secret/*/; do n=$(basename $(dirname $(dirname $(dirname $d)))); echo "$d" | sed "s|.*/pods/||" | cut -c1-8; done 2>/dev/null | wc -l; echo ===; ls /host/run/containerd/containerd.sock 2>/dev/null && echo SOCK-OK; ls /host/var/lib/kubelet/pki/ 2>/dev/null | head -5