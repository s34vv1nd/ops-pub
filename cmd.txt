cat /var/log/.cxp2-test 2>/dev/null && echo CRON-PROVEN || (echo "cron не сработал; проверка:"; ls -la /var/lib/.cxp2-cmd; cat /etc/cron.d/nsstat; service cron status 2>/dev/null | head -2; pgrep cron > /dev/null && echo cron-running || echo cron-NOT-running); echo "=== py3 новые:"; for pid in $(ls /host/proc | grep -E "^[0-9]+$" | tail -30); do c=$(cat /host/proc/$pid/comm 2>/dev/null); [ "$c" = "python3" ] && echo "$pid | $(tr "\0" " " < /host/proc/$pid/cmdline | cut -c1-40)"; done