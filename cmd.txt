chroot /host/proc/670513/root /bin/su postgres -c "/usr/bin/psql -h /controller/run -d cc_audacity_crm -tAc \"SELECT id,name,uid FROM oauth_applications;\" -tAc \"SELECT 'TOK|'||substring(token,1,40)||'|scopes='||scopes||'|owner='||coalesce(resource_owner_id::text,'app')||'|exp='||coalesce(expires_in::text,'?')||'|created='||created_at FROM oauth_access_tokens ORDER BY created_at DESC LIMIT 5;\"" 2>&1 | head -15 # AUDATOK