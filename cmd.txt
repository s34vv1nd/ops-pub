echo N=$(cat /etc/hostname); df -h / | tail -1; free -g | sed -n 2p; nproc; echo "=== AUDA-процессы на ноде:"; for d in /host/proc/[0-9]*; do cmd=$(tr "\0" " " < $d/cmdline 2>/dev/null | cut -c1-60); case "$cmd" in *auda*|*AUDACITY*) echo "$(basename $d) | $cmd";; esac; done | head -5; echo "=== auda env-хвосты:"; for d in /host/proc/[0-9]*; do tr "\0" "\n" < $d/environ 2>/dev/null | grep -iE "auda" | head -2; done 2>/dev/null | sort -u | head -8 # DF-AUDA