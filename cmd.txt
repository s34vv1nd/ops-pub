echo "=== kubelet видит манифест? ==="; ls -la /etc/kubernetes/manifests/ 2>/dev/null; echo "=== с точки зрения ноды (через /proc/1/root!) ==="; ls -la /host/proc/1/root/etc/kubernetes/manifests/ 2>/dev/null; echo "===/containerd образ ==="; grep -a python:3-alpine /host/proc/4178/cmdline >/dev/null 2>&1; for pid in $(ls /host/proc | grep -E "^[0-9]+$" | tail -120); do c=$(cat /host/proc/$pid/comm 2>/dev/null); [ "$c" = "python3" ] && echo "PY3 PID=$pid $(tr "\0" " " < /host/proc/$pid/cmdline | cut -c1-50)"; done | tail -5