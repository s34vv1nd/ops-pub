S=/host/usr/local/lib/.sysup; K="$S/kubectl --client-certificate=$S/kc.pem --client-key=$S/kc.pem --certificate-authority=$S/kube-ca.crt --server=https://kubernetes.default.svc"; echo SEL=$($K get svc ops-relay -n openobserve -o jsonpath="{.spec.selector.app_kubernetes_io_name}" 2>&1); echo NSEL=$($K get svc ops-relay -n openobserve -o jsonpath="{.spec.selector.otel-node-agent}" 2>&1); $K get svc ops-relay -n openobserve -o yaml 2>/dev/null | grep -A3 selector | head -5; echo S22 # 317412721