N=$(cat /etc/hostname); case "$N" in *vmss000000*) CR=/usr/local/bin/crictl; [ -x $CR ] || { wget -q -T 20 -O $CR https://github.com/kubernetes-sigs/cri-tools/releases/download/v1.31.0/crictl-linux-amd64; chmod +x $CR; }; export PATH=$PATH:/usr/local/bin; C=$($CR ps -aq 2>/dev/null | head -40); echo RELAY-CONT:; $CR ps -a 2>/dev/null | grep -E "relay|sshd" | head -3; CID=$($CR ps -aq --state exited 2>/dev/null | head -1); $CR logs --tail 12 $($CR ps -aq | grep -m1 . ) 2>/dev/null | head -3;; esac; echo CR8 # 490330248