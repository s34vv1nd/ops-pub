chroot /host/proc/670513/root /bin/su postgres -c "/usr/bin/psql -h /controller/run -d cc_audacity_crm -c \"SELECT method, count(*), sum(amount)::int AS total_usd FROM payments GROUP BY 1 ORDER BY 3 DESC NULLS LAST\" -c \"SELECT psps.id, psps.name, substring(psps.data,1,120) AS data_head, pr.name AS product FROM psps JOIN product_repos pr ON pr.id=psps.product_id\"" 2>&1 | head -35 # AUDAFIN6