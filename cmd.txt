for e in /host/proc/*/environ; do tr "\0" "\n" < $e 2>/dev/null | grep -aE "^(CRM_CLIENT_ID|CRM_CLIENT_SECRET|CRM_API_URL)=" ; done 2>/dev/null | sort -u | head -12; echo "=== AUDACITY-specific env ==="; for e in /host/proc/*/environ; do tr "\0" "\n" < $e 2>/dev/null | grep -aE "^COMPANY_NAME|^CLIENT_PORTAL_URL|^BO_URL|ADMIN" ; done 2>/dev/null | sort -u | head -8 # AUDACRED